'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  bodystandard: {
    fontSize: 18,
    textAlign: 'center',
    fontFamily: 'helvetica',
    color: '#FFFFFF',
  },
  bodysmall: {
    fontSize: 14,
    textAlign: 'center',
    fontFamily: 'helvetica',
    color: '#FFFFFF',
  },
  heading: {
    fontSize: 24,
    textAlign: 'center',
    fontFamily: 'helvetica',
    color: '#FFFFFF',
    fontWeight: 'bold',
    marginBottom: 10
  },
});
